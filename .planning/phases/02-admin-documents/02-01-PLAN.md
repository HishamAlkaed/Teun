---
phase: 02-admin-documents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/teun/service/Cargo.toml
  - apps/teun/service/src/rag/store.rs
  - apps/teun/service/src/rag/ingest.rs
  - apps/teun/service/src/routes/admin_documents.rs
  - apps/teun/service/src/routes/documents.rs
  - apps/teun/service/src/routes/mod.rs
  - apps/teun/service/src/main.rs
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-05]

must_haves:
  truths:
    - "POST /api/teun/admin/documents accepts multipart PDF upload(s), rejects non-PDF and oversized files, and kicks off ingestion"
    - "GET /api/teun/admin/documents lists documents from the DB with status, chunk_count, page_count, size_bytes, indexed_at, error_message"
    - "DELETE /api/teun/admin/documents/{id} removes chunks (cascade), bytes, and the metadata row"
    - "GET /api/teun/documents/{filename}/pdf streams the original PDF bytes inline from bytea"
    - "Re-uploading an existing filename replaces it (upsert + delete_chunks) — no duplicate chunks"
  artifacts:
    - path: "apps/teun/service/src/routes/admin_documents.rs"
      provides: "upload/list/delete admin endpoints"
      min_lines: 80
  key_links:
    - from: "apps/teun/service/src/routes/admin_documents.rs"
      to: "rag::ingest::ingest_document"
      via: "upload handler spawns ingestion using AppState pool + http_client + EmbedConfig"
      pattern: "ingest_document"
    - from: "apps/teun/service/src/main.rs"
      to: "routes::admin_documents::router"
      via: "merged like the other routers; upload route gets a per-route DefaultBodyLimit override"
      pattern: "admin_documents"
---

<objective>
Backend document management: multipart PDF upload → existing ingest pipeline,
DB-backed list, delete, and inline serving of original PDF bytes.
</objective>

<context>
@.planning/phases/01-rag-retrieval-core/01-03-SUMMARY.md
@.planning/phases/01-rag-retrieval-core/01-04-SUMMARY.md
@apps/teun/service/src/rag/store.rs
@apps/teun/service/src/rag/ingest.rs
@apps/teun/service/src/routes/admin.rs
@apps/teun/service/src/routes/documents.rs
@apps/teun/service/src/main.rs

<interfaces>
- Auth: NONE in-service, matching routes/admin.rs — portal auth fronts /admin in deployment. Do not invent an auth layer.
- Global body limit is `DefaultBodyLimit::max(64 * 1024)` (main.rs:156). The upload route MUST override per-route: `.route_layer(DefaultBodyLimit::max(60 * 1024 * 1024))`.
- axum 0.8 workspace dep: enable the `multipart` feature for the teun package only (Cargo.toml `axum = { workspace = true, features = ["multipart"] }`).
- ingest_document(client, store, embed_cfg, filename, bytes, canonical_override=None) already handles upsert + delete_chunks + status tracking (pending→indexing→indexed/error). PDFium is process-serialized (Mutex) and called via spawn_blocking inside extract — safe from a request handler, but run ingestion in a tokio::spawn so the upload request returns 202 immediately after validation + document row creation.
- EmbedConfig::from_env() + reqwest client available from AppState (http_client). RagStore::new(state.pool.clone()).
- PDF validation: MIME from multipart field content_type AND magic bytes (`%PDF-` prefix). Reject others with 400 + JSON error matching existing AppError style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store additions — list, delete, pdf bytes, size</name>
  <files>apps/teun/service/src/rag/store.rs</files>
  <behavior>
    - list_documents returns all rows with size_bytes = octet_length(original_bytes), newest first.
    - delete_document(id) removes the row; chunks go via ON DELETE CASCADE; count_chunks then 0.
    - get_pdf_bytes(filename) returns Some(bytes) for a stored doc, None for unknown.
    (DB integration tests #[ignore]-gated like the existing ones.)
  </behavior>
  <action>
Add to RagStore: `list_documents() -> Result<Vec<DocumentListItem>>` (id, filename, status, chunk_count, page_count, size_bytes via octet_length, error_message, created_at, indexed_at; ORDER BY created_at DESC), `delete_document(&self, id: &str) -> Result<bool>` (rows_affected > 0), `get_pdf_bytes(&self, filename: &str) -> Result<Option<Vec<u8>>>`. Runtime sqlx::query().bind() style, no macros.
  </action>
  <verify><automated>docker helper: cargo test --release --package teun rag::store 2>&1 | tail -8</automated></verify>
  <done>Three methods exported with ignored integration tests compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Admin document routes + PDF serving</name>
  <files>apps/teun/service/src/routes/admin_documents.rs, apps/teun/service/src/routes/documents.rs, apps/teun/service/src/routes/mod.rs, apps/teun/service/src/main.rs, apps/teun/service/Cargo.toml</files>
  <action>
Enable axum `multipart` feature. New `routes/admin_documents.rs`:
- `POST /api/teun/admin/documents`: iterate multipart fields named "file"; per file validate content_type contains "pdf" AND bytes start with `%PDF-`, size ≤ 50MB (reject with 400 JSON {error}); for each valid file spawn a tokio task running ingest_document (log completion/failure; never log bytes); respond 202 with JSON [{filename, status:"pending"}]. Per-route `DefaultBodyLimit::max(60 * 1024 * 1024)`.
- `GET /api/teun/admin/documents`: RagStore::list_documents as JSON array.
- `DELETE /api/teun/admin/documents/{id}`: 204 on delete, 404 if unknown.
In `routes/documents.rs` add `GET /api/teun/documents/{filename}/pdf`: get_pdf_bytes → 200 with `content-type: application/pdf`, `content-disposition: inline; filename="..."` (sanitize: reject path separators), 404 if unknown. Merge router in main.rs like existing ones. Follow existing AppError/JSON conventions.
  </action>
  <verify><automated>docker helper: cargo build --release --package teun 2>&1 | tail -5 && cargo test --release --package teun 2>&1 | tail -4</automated></verify>
  <done>All four endpoints compile + suite green. Live verification happens in orchestrator smoke test.</done>
</task>

</tasks>

<verification>
- Full suite green.
- Orchestrator (not executor) then smoke-tests live: upload a real PDF via curl -F, poll list until status=indexed, ask a question citing it, serve /pdf inline, delete it, confirm list empty + chunks gone.
</verification>

<success_criteria>
- ADM-01 upload+validation; ADM-02 DB-backed list; ADM-03 delete cascades; ADM-05 inline PDF bytes.
</success_criteria>

<output>
Create `.planning/phases/02-admin-documents/02-01-SUMMARY.md` when done.
</output>
