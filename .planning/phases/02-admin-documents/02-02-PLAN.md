---
phase: 02-admin-documents
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/teun/web/src/components/DocsTab.tsx
  - apps/teun/web/src/components/DocumentManager.tsx
  - apps/teun/web/src/lib/api.ts
autonomous: true
requirements: [ADM-04]

must_haves:
  truths:
    - "The Documentatie admin tab shows a document manager: upload (multi-file), list with status/chunk_count/size, delete with confirm"
    - "Ingestion status refreshes (poll while any document is pending/indexing) without manual reload"
    - "The existing static help content is preserved, reachable from the same tab (collapsible section below the manager)"
    - "Visual style matches the existing admin tabs (EvalTab/questions patterns, same api.ts wrapper conventions)"
  artifacts:
    - path: "apps/teun/web/src/components/DocumentManager.tsx"
      provides: "upload/list/delete UI"
      min_lines: 80
---

<objective>
Replace the static Documentatie placeholder with a working document manager,
keeping the help content. ADM-04.
</objective>

<context>
@apps/teun/web/src/components/DocsTab.tsx
@apps/teun/web/src/components/EvalTab.tsx
@apps/teun/web/src/components/AdminLayout.tsx
@apps/teun/web/src/lib/api.ts

<interfaces>
Backend endpoints from plan 02-01 (all JSON unless noted):
- POST /api/teun/admin/documents — multipart field name "file" (repeatable); 202 → [{filename, status:"pending"}]; 400 → {error}
- GET /api/teun/admin/documents — [{id, filename, status, chunk_count, page_count, size_bytes, error_message, created_at, indexed_at}]
- DELETE /api/teun/admin/documents/{id} — 204 / 404
- GET /api/teun/documents/{filename}/pdf — original PDF inline (use as "open source" link target)
Status values: pending | indexing | indexed | error (error shows error_message).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocumentManager component + tab rewire</name>
  <files>apps/teun/web/src/components/DocumentManager.tsx, apps/teun/web/src/components/DocsTab.tsx, apps/teun/web/src/lib/api.ts</files>
  <action>
Add api.ts helpers (listAdminDocuments, uploadDocuments(FormData), deleteDocument) following existing wrapper style. Build DocumentManager: file input (accept="application/pdf", multiple) + drag-drop zone; upload button with busy state; table of documents (filename, human-readable size, pages, chunks, status badge, indexed_at, delete button with window.confirm); filename links to `/api/teun/documents/{filename}/pdf` (target=_blank); poll GET every 3s while any doc is pending/indexing, stop when stable; surface error status rows with error_message tooltip/inline. Rework DocsTab: DocumentManager on top, existing help sections preserved below inside a collapsible ("Help & uitleg") defaulting closed. Match existing component/styling idioms (reuse EvalTab table/button classes; no new CSS framework). Dutch labels consistent with the rest of the admin.
  </action>
  <verify><automated>cd apps/teun/web && npm run build 2>&1 | tail -5</automated></verify>
  <done>Web build green; tab renders manager + collapsible help.</done>
</task>

</tasks>

<verification>
- `npm run build` green (typecheck included).
- Orchestrator verifies live in the running production-image container after rebuild.
</verification>

<success_criteria>
- ADM-04: functional document-manager tab, help content preserved.
</success_criteria>

<output>
Create `.planning/phases/02-admin-documents/02-02-SUMMARY.md` when done.
</output>
