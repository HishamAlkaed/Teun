---
phase: 03-citation-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/teun/service/src/routes/documents.rs
  - apps/teun/web/src/chat/components/DocumentViewer.tsx
autonomous: true
requirements: [CIT-01, CIT-02, CIT-03]

must_haves:
  truths:
    - "GET /api/teun/documents lists documents from the DB (indexed docs), not the resources dir"
    - "GET /api/teun/documents/{filename}?line_range=X-Y returns the DB extracted_text in the SAME JSON shape the endpoint returns today (content + highlight fields), disk .md fallback kept for unknown filenames"
    - "DocumentViewer opens a PDF-sourced document showing extracted text with the cited line range highlighted; PDF-sourced docs default to the 'lines' view (extracted text is not markdown)"
    - "SSE ChatEvent stream and MortgageAnswer/SourceReference shapes untouched — no changes to types.rs, stream.rs, chat.rs"
  key_links:
    - from: "apps/teun/service/src/routes/documents.rs"
      to: "RagStore::get_extracted_text / list_documents"
      via: "DB-first lookup with disk fallback"
      pattern: "get_extracted_text"
---

<objective>
Point the document read endpoints at the DB canonical text so citations open
correctly (CIT-02); CIT-01 was already delivered by commit 06fe9d6 (verifier
reads documents.extracted_text) — verify, don't re-implement. CIT-03 is a
regression gate: contracts stay byte-identical.
</objective>

<context>
@apps/teun/service/src/routes/documents.rs
@apps/teun/service/src/rag/store.rs
@apps/teun/service/src/judge/verifier.rs
@apps/teun/web/src/chat/components/DocumentViewer.tsx

<interfaces>
- Current GET /api/teun/documents/{filename} JSON shape (documents.rs:75-133): preserve field names/types exactly; only the content SOURCE changes (DB extracted_text first, disk read fallback). Frontend already renders content + highlight bounds.
- Add a `content_type` field ONLY if the existing shape has room for additive fields without breaking the viewer (it tolerates unknown fields) — use it to signal "pdf_text" vs "markdown" so the viewer can pick the default view mode.
- DocumentViewer (DocumentViewer.tsx:24) has "rendered"|"lines" modes; extracted PDF text must default to "lines".
- Do NOT touch agent/types.rs, agent/stream.rs, routes/chat.rs, judge/* — CIT-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-backed document endpoints (shape-preserving)</name>
  <files>apps/teun/service/src/routes/documents.rs</files>
  <action>
List endpoint: return indexed documents from RagStore::list_documents (filenames), falling back to/merging the existing .md dir listing for files not in the DB (transition safety). Content endpoint: resolve via RagStore::get_extracted_text(filename) first; if None, keep today's disk read; preserve the response JSON shape byte-for-byte, adding only an additive `content_type: "pdf_text"|"markdown"` field. Keep the path traversal guard. line_range/highlight computation unchanged.
  </action>
  <verify><automated>docker helper: cargo build --release --package teun 2>&1 | tail -4 && cargo test --release --package teun 2>&1 | tail -4</automated></verify>
  <done>Endpoints DB-first with fallback, shape preserved, suite green.</done>
</task>

<task type="auto">
  <name>Task 2: DocumentViewer default view for PDF text</name>
  <files>apps/teun/web/src/chat/components/DocumentViewer.tsx</files>
  <action>
Read the additive content_type field; when "pdf_text", default the view mode to "lines" (user can still toggle). No other behavior changes. npm run build must pass.
  </action>
  <verify><automated>cd apps/teun/web && npm run build 2>&1 | tail -4</automated></verify>
  <done>Viewer defaults sensibly per content type; build green.</done>
</task>

<task type="auto">
  <name>Task 3: CIT-03 regression evidence</name>
  <files>none (verification only)</files>
  <action>
Run the full Rust suite + web build. Then `git diff --stat` against the phase start for agent/types.rs, agent/stream.rs, routes/chat.rs, judge/ — must show ZERO changes to those files this phase. Record the evidence in the SUMMARY. (Orchestrator re-runs the live E2E SSE-shape check afterwards.)
  </action>
  <verify><automated>docker helper: cargo test --release --package teun 2>&1 | tail -4</automated></verify>
  <done>Suite green; untouched-contract evidence recorded.</done>
</task>

</tasks>

<success_criteria>
- CIT-01: already satisfied (verifier reads extracted_text — confirm via existing tests, no code change).
- CIT-02: DocumentViewer opens PDF-sourced text with correct highlight.
- CIT-03: contract files untouched; suite + build green; live E2E re-verified by orchestrator.
</success_criteria>

<output>
Create `.planning/phases/03-citation-integrity/03-01-SUMMARY.md` when done.
</output>
